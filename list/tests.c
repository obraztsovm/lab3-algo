#include "generic.h"
#include <assert.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>

/*
===============================================================================
Тест 1: test_create_and_append_ints
Назначение: Проверяет создание списка, добавление int элементов и порядок.
Шаги:
1. Создаём список для int.
2. Добавляем 3 элемента.
3. Проверяем длину списка.
4. Проверяем порядок элементов.
5. Освобождаем память списка.
Ожидаемый результат:
- Длина = 3
- Порядок элементов соответствует вставке
- Без утечек памяти
===============================================================================
*/
void test_create_and_append_ints()
{
    printf("Test 1: createList() + appendItem() with int ... ");
    // TODO - имплементировать
    printf("OK\n");
}

/*
===============================================================================
Тест 2: test_append_floats
Назначение: Проверяет appendItem с float.
Шаги:
1. Создаём список для float.
2. Добавляем 3 элемента.
3. Проверяем длину и порядок элементов.
4. Освобождаем память списка.
Ожидаемый результат:
- Длина = 3
- Порядок float элементов соответствует вставке
===============================================================================
*/
void test_append_floats()
{
    printf("Test 2: appendItem() with float ... ");
    // TODO - имплементировать
    printf("OK\n");
}

/*
===============================================================================
Тест 3: test_empty_list_length
Назначение: Проверяет listLength на пустом списке.
Шаги:
1. Создаём пустой список int.
2. Проверяем длину списка.
3. Освобождаем память списка.
Ожидаемый результат:
- listLength = 0
===============================================================================
*/
void test_empty_list_length()
{
    printf("Test 3: listLength() on empty list ... ");
    // TODO - имплементировать
    printf("OK\n");
}

/*
===============================================================================
Тест 4: test_find_and_pop
Назначение: Проверяет findItem и popItem.
Шаги:
1. Создаём список int и добавляем 10 элементов.
2. Находим индекс элемента 7.
3. Удаляем элемент с этим индексом с помощью popItem.
4. Проверяем возвращённое значение и корректное уменьшение длины списка.
5. Проверяем, что findItem для удалённого элемента возвращает -1.
Ожидаемый результат:
- findItem возвращает правильный индекс
- popItem возвращает корректное значение
- длина списка уменьшается на 1
===============================================================================
*/
void test_find_and_pop()
{
    printf("Test 4: findItem() + popItem() ... ");
    // TODO - имплементировать
    printf("OK\n");
}

/*
===============================================================================
Тест 5: test_memory_freeing
Назначение: Проверка безопасного освобождения списка.
Шаги:
1. Создаём список и добавляем 100 элементов.
2. Освобождаем список.
Ожидаемый результат:
- Нет падений программы.
===============================================================================
*/
void test_memory_freeing()
{
    printf("Test 5: freeList() safety check (small) ... ");
    // TODO - имплементировать

    printf("OK\n");
}

/*
===============================================================================
Тест 6: test_memory_with_inner_allocs
Назначение: Проверка корректного освобождения памяти для элементов,
которые сами выделяют динамическую память.
Шаги:
1. Создаём список указателей (char *).
2. Выделяем и добавляем 10_000 строк.
3. Освобождаем внутренние строки вручную.
4. Освобождаем список.
Ожидаемый результат:
- Нет утечек памяти
===============================================================================
*/
void test_memory_with_inner_allocs()
{
    printf("Test 6: inner-heap allocations + proper freeing ... ");
    // TODO - имплементировать
    printf("OK\n");
}

/*
===============================================================================
Тест 7: test_mass_append_api
Назначение: Проверка массового добавления 100_000 элементов через appendItem.
Шаги:
1. Создаём список int.
2. Добавляем 100_000 элементов.
3. Проверяем длину списка.
4. Проверяем значения первого, среднего и последнего элемента.
5. Освобождаем память.
Ожидаемый результат:
- Все элементы добавлены в правильном порядке
- Длина списка = 100_000
- Выполнение теста без утечек памяти
===============================================================================
*/
void test_mass_append_api()
{
    printf("Test 7: mass append using appendItem() (100k) ... ");
    // TODO - имплементировать
    printf("OK\n");
}

/*
===============================================================================
Тест 8: test_mass_1m_fast
Назначение: Стресс-тест на 1_000_000 элементов (O(N)).
- Использует прямое выделение и связывание узлов для ускорения (не через appendItem).
Шаги:
1. Создаём список int.
2. Выделяем и связываем 1_000_000 узлов.
3. Проверяем первый, средний и последний элементы.
4. Проверяем popItem для начала, середины и конца списка.
5. Освобождаем список.
Ожидаемый результат:
- Все элементы корректно созданы
- popItem корректно удаляет элементы
- Длина и значения корректны
- Без утечек памяти
===============================================================================
*/
void test_mass_1m_fast()
{
    printf("Test 8: mass append FAST (1,000,000 elements) ... ");
    // TODO - имплементировать
    printf("OK\n");
}

/*
===============================================================================
Тест 8b: test_mass_1m_profiled
Назначение: Стресс-тест на 1_000_000 элементов с подсчётом памяти и проверкой освобождения.
Особенности:
- Считаем количество выделенных узлов и внутренней памяти
- После freeList проверяем, что все узлы освобождены
===============================================================================
*/
void test_mass_1m_profiled()
{
    printf("Test 8b: mass append FAST + memory profiling (1,000,000 elements) ... ");
    // TODO - имплементировать
    printf("OK\n");
}

int main()
{
    printf("Running GenericList unit tests...\n\n");

    clock_t total_start = clock();

    test_create_and_append_ints();
    test_append_floats();
    test_empty_list_length();
    test_find_and_pop();
    test_memory_freeing();
    test_memory_with_inner_allocs();
    test_mass_append_api();
    test_mass_1m_fast();
    test_mass_1m_profiled(); // Тест с подсчётом памяти

    clock_t total_end = clock();
    double total_secs = (double)(total_end - total_start) / CLOCKS_PER_SEC;

    printf("\nAll tests passed successfully!\n");
    printf("Total test duration: %.3f seconds\n", total_secs);

    return 0;
}
