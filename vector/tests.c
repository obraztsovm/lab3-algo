#include "generic.h"
#include <assert.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <time.h>

/*
===============================================================================
Тест 1: test_create_and_append_ints
Назначение: Проверяет создание вектора, добавление int элементов и порядок.
Шаги:
1. Создаём вектор для int.
2. Добавляем 3 элемента.
3. Проверяем размер вектора.
4. Проверяем порядок элементов.
5. Освобождаем память вектора.
Ожидаемый результат:
- Размер = 3
- Порядок элементов соответствует вставке
- Без утечек памяти
===============================================================================
*/
void test_create_and_append_ints()
{
    printf("Test 1: createVector() + appendVectorItem() with int ... ");
    // TODO - написать тесты для функции
    printf("OK\n");
}

/*
===============================================================================
Тест 2: test_append_floats
Назначение: Проверяет appendVectorItem с float.
Шаги:
1. Создаём вектор для float.
2. Добавляем 3 элемента.
3. Проверяем размер и порядок элементов.
4. Освобождаем память вектора.
Ожидаемый результат:
- Размер = 3
- Порядок float элементов соответствует вставке
===============================================================================
*/
void test_append_floats()
{
    printf("Test 2: appendVectorItem() with float ... ");
    // TODO - написать тесты для функции
    printf("OK\n");
}

/*
===============================================================================
Тест 3: test_empty_vector_size
Назначение: Проверяет размер пустого вектора.
Шаги:
1. Создаём пустой вектор int.
2. Проверяем размер вектора.
3. Освобождаем память вектора.
Ожидаемый результат:
- size = 0
- capacity >= MIN_SIZE
===============================================================================
*/
void test_empty_vector_size()
{
    printf("Test 3: size on empty vector ... ");
    // TODO - написать тесты для функции
    printf("OK\n");
}

/*
===============================================================================
Тест 4: test_get_set_items
Назначение: Проверяет getVectorItem и setVectorItem.
Шаги:
1. Создаём вектор int и добавляем 5 элементов.
2. Изменяем значение элемента с индексом 2 с помощью setVectorItem.
3. Проверяем, что значение изменилось корректно.
4. Проверяем остальные элементы не изменились.
Ожидаемый результат:
- setVectorItem корректно обновляет элемент
- getVectorItem возвращает правильные значения
===============================================================================
*/
void test_get_set_items()
{
    printf("Test 4: getVectorItem() + setVectorItem() ... ");
    // TODO - написать тесты для функции
    printf("OK\n");
}

/*
===============================================================================
Тест 5: test_find_and_pop
Назначение: Проверяет findVectorItem и popVectorItem.
Шаги:
1. Создаём вектор int и добавляем 10 элементов.
2. Находим индекс элемента 7.
3. Удаляем элемент с этим индексом с помощью popVectorItem.
4. Проверяем возвращённое значение и корректное уменьшение размера.
5. Проверяем, что findVectorItem для удалённого элемента возвращает -1.
Ожидаемый результат:
- findVectorItem возвращает правильный индекс
- popVectorItem возвращает корректное значение
- размер вектора уменьшается на 1
===============================================================================
*/
void test_find_and_pop()
{
    printf("Test 5: findVectorItemCmp() + popVectorItem() ... ");
    // TODO - написать тесты для функции
    printf("OK\n");
}

/*
===============================================================================
Тест 6: test_memory_freeing
Назначение: Проверка безопасного освобождения вектора.
Шаги:
1. Создаём вектор и добавляем 1000 элементов.
2. Освобождаем вектор.
Ожидаемый результат:
- Нет падений программы.
===============================================================================
*/
void test_memory_freeing()
{
    printf("Test 6: vectorFree() safety check (small) ... ");
    // TODO - написать тесты для функции
    printf("OK\n");
}

/*
===============================================================================
Тест 7: test_memory_with_inner_allocs
Назначение: Проверка корректного освобождения памяти для элементов,
которые сами выделяют динамическую память.
Шаги:
1. Создаём вектор указателей (char *).
2. Выделяем и добавляем 10_000 строк.
3. Освобождаем внутренние строки вручную.
4. Освобождаем вектор.
Ожидаемый результат:
- Нет утечек памяти
===============================================================================
*/
void test_memory_with_inner_allocs()
{
    printf("Test 8: inner-heap allocations + proper freeing ... ");
    // TODO - написать тесты для функции
    printf("OK\n");
}

/*
===============================================================================
Тест 8: test_auto_resize
Назначение: Проверка автоматического расширения вектора.
Шаги:
1. Создаём вектор с начальной capacity = MIN_SIZE.
2. Добавляем элементы до тех пор, пока не произойдёт несколько расширений.
3. Проверяем, что capacity увеличивается.
4. Проверяем корректность всех элементов.
Ожидаемый результат:
- capacity увеличивается при необходимости
- все элементы сохраняются корректно
===============================================================================
*/
void test_auto_resize()
{
    printf("Test 9: automatic capacity resize ... ");
    // TODO - написать тесты для функции
    printf("OK\n");
}

/*
===============================================================================
Тест 9: test_mass_append
Назначение: Проверка массового добавления 100_000 элементов.
Шаги:
1. Создаём вектор int.
2. Добавляем 100_000 элементов.
3. Проверяем размер вектора.
4. Проверяем значения первого, среднего и последнего элемента.
5. Освобождаем память.
Ожидаемый результат:
- Все элементы добавлены в правильном порядке
- Размер вектора = 100_000
- Выполнение теста без утечек памяти
===============================================================================
*/
void test_mass_append()
{
    printf("Test 10: mass append (100k) ... ");
    // TODO - написать тесты для функции
    printf("OK\n");
}

/*
===============================================================================
Тест 10: test_mass_1m
Назначение: Стресс-тест на 1_000_000 элементов.
Шаги:
1. Создаём вектор int.
2. Добавляем 1_000_000 элементов.
3. Проверяем первый, средний и последний элементы.
4. Проверяем popVectorItem для начала, середины и конца.
5. Освобождаем вектор.
Ожидаемый результат:
- Все элементы корректно добавлены
- popVectorItem корректно удаляет элементы
- Размер и значения корректны
- Без утечек памяти
===============================================================================
*/
void test_mass_1m()
{
    printf("Test 11: mass append (1,000,000 elements) ... ");
    // TODO - написать тесты для функции
    printf("OK\n");
}

/*
===============================================================================
Тест 11: test_mass_1m_profiled
Назначение: Стресс-тест на 1_000_000 элементов с подсчётом памяти.
Особенности:
- Считаем объём выделенной памяти
- Проверяем корректность данных
- После vectorFree проверяем, что вся память освобождена
===============================================================================
*/
void test_mass_1m_profiled()
{
    printf("Test 12: mass append + memory profiling (1,000,000 elements) ... ");
    // TODO - написать тесты для функции
    // выведите что-то вроде:
    // size_t memory_used = vector->capacity * vector->elem_size;
    // printf("\nCapacity: %zu, Memory used: %.2f MB\n",
    //       vector->capacity, memory_used / (1024.0 * 1024.0));

    printf("Memory freed successfully\n");
    printf("OK\n");
}

/*
===============================================================================
Тест 12: test_boundary_cases
Назначение: Проверка граничных случаев (пустой вектор, выход за границы).
Шаги:
1. Проверяем getVectorItem на пустом векторе (должен вернуть NULL).
2. Проверяем popVectorItem на пустом векторе (должен вернуть NULL).
3. Проверяем getVectorItem с индексом >= size (должен вернуть NULL).
4. Проверяем setVectorItem с индексом >= capacity (должен вернуть -1).
Ожидаемый результат:
- Все граничные случаи обрабатываются корректно
===============================================================================
*/
void test_boundary_cases()
{
    printf("Test 13: boundary cases ... ");
    // TODO - написать тесты для функции
    printf("OK\n");
}

int main()
{
    printf("Running Vector unit tests...\n\n");

    clock_t total_start = clock();

    test_create_and_append_ints();
    test_append_floats();
    test_empty_vector_size();
    test_get_set_items();
    test_find_and_pop();
    test_memory_freeing();
    test_memory_with_inner_allocs();
    test_auto_resize();
    test_mass_append();
    test_mass_1m();
    test_mass_1m_profiled();
    test_boundary_cases();

    clock_t total_end = clock();
    double total_secs = (double)(total_end - total_start) / CLOCKS_PER_SEC;

    printf("\nAll tests passed successfully!\n");
    printf("Total test duration: %.3f seconds\n", total_secs);

    return 0;
}